#!/bin/sh
# aaron — run the coding agent
#
# Usage:
#   aaron                                        interactive REPL
#   aaron "prompt"                               single-shot
#   aaron skill list                             list installed skills
#   aaron skill show <name>                      print a skill
#   aaron skill create <name> "eval task"        create via RSI
#   aaron skill improve <name> "eval task"       improve via RSI
#   aaron skill rsi <name> "eval task"           create-or-improve via RSI
#   aaron skill ... --budget N                   set RSI budget (default 3)
#   aaron workflow list                          list defined workflows
#   aaron workflow create <name> "goal"          define a new workflow
#   aaron workflow improve <name> "feedback"     revise step prompts
#   aaron workflow run <name>                    run or resume a workflow
#   aaron :workflow <sub> ...                    colon prefix also works
exec node "/Users/ml5174/Development/github/aaron/agent-harness.mjs" "$@"
